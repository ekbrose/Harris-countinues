[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "gunicorn server:app --bind 0.0.0.0:$PORT --workers 2 --timeout 300 --log-level info"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

[[services]]
name = "harris-scraper-api"
